// content.js — deprecated. Kept only so nothing that still references it breaks.
// The real logic now lives in content-core/utils.js, content-providers/*.js,
// and content-core/core.js, all declared as content_scripts in manifest.json
// so chrome.runtime is available to them.
//
// Nothing to do here.
